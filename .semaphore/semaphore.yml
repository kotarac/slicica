version: v1.0
name: semaphore
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: node
    task:
      jobs:
        - name: test
          commands:
            - checkout
            - yarn install
            - yarn run test
